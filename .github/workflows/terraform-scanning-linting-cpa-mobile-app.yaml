name: Terraform Scanning & Linting (CPA-Mobile-App)

on:
  push:
    branches:
      - main
    paths:
      - 'CPA-Mobile-App/**/*.tf'
  workflow_dispatch:

env:
  TF_VERSION: '1.12.2'
  CHECKOV_SKIP_RULES: 'CKV_TF_1,CKV_TF_2,CKV2_AZURE_21,CKV_AZURE_100,CKV_AZURE_140,CKV_AZURE_211,CKV_AZURE_225,CKV_AZURE_213,CKV_AZURE_71,CKV_AZURE_65,CKV_AZURE_13,CKV_AZURE_66,CKV_AZURE_16,CKV_AZURE_63,CKV_AZURE_88,CKV_AZURE_17,CKV_AZURE_112,CKV_AZURE_224,CKV_AZURE_229,CKV_AZURE_206,CKV_AZURE_109,CKV_AZURE_244,CKV_AZURE_33,CKV2_AZURE_2,CKV2_AZURE_38,CKV2_AZURE_40,CKV_AZURE_24,CKV_AZURE_23,CKV_AZURE_35,CKV_AZURE_36,CKV_AZURE_132,CKV_AZURE_203'

jobs:
  terraform-scan-cpa-mobile-app:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Terraform CLI
        run: |
          echo "Installing Terraform ${TF_VERSION}..."
          wget https://releases.hashicorp.com/terraform/${TF_VERSION}/terraform_${TF_VERSION}_linux_amd64.zip
          unzip terraform_${TF_VERSION}_linux_amd64.zip
          sudo mv terraform /usr/local/bin/
          terraform -version

      - name: Find all Terraform files in CPA-Mobile-App
        run: |
          echo "GITHUB_WORKSPACE: $GITHUB_WORKSPACE"
          find "$GITHUB_WORKSPACE/CPA-Mobile-App" -name '*.tf' -exec echo "Found Terraform file: {}" \;

      - name: Terraform Init & Validate (CPA-Mobile-App)
        working-directory: CPA-Mobile-App
        run: |
          terraform init -backend=false
          terraform validate

      - name: Install Snyk CLI
        run: |
          echo "Installing Snyk CLI..."
          curl --compressed https://static.snyk.io/cli/latest/snyk-linux -o snyk
          chmod +x ./snyk
          sudo mv ./snyk /usr/local/bin/
          snyk --version

      - name: Snyk IaC Scan - CPA-Mobile-App
        working-directory: CPA-Mobile-App
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        run: |
          echo "Running Snyk IaC test for CPA-Mobile-App..."
          snyk iac test --severity-threshold=high --report --policy-path="$GITHUB_WORKSPACE/.snyk" --exclude=.terraform,.git || true

      - name: Run TFLint-CPA-Mobile-App
        working-directory: CPA-Mobile-App
        continue-on-error: true
        run: |
          curl -s https://raw.githubusercontent.com/terraform-linters/tflint/master/install_linux.sh | bash
          tflint --version

          set -e

          for dir in $(find . -type f -name "*.tf" -exec dirname {} \; | sort -u); do
            echo "Running TFLint in $dir"
            (cd "$dir" && tflint --init && tflint)
          done

      - name: Run tfsec-CPA-Mobile-App
        working-directory: CPA-Mobile-App
        continue-on-error: true
        run: |
          curl -s https://raw.githubusercontent.com/aquasecurity/tfsec/master/scripts/install_linux.sh | bash
          tfsec --exclude azure-database-enable-audit

      - name: Run Checkov-CPA-Mobile-App
        working-directory: CPA-Mobile-App
        continue-on-error: true
        run: |
          pip install checkov
          checkov -d . --download-external-modules true --skip-check "$CHECKOV_SKIP_RULES" --compact --quiet

      - name: Download and Install Latest Terrascan
        run: |
          curl -L "$(curl -s https://api.github.com/repos/tenable/terrascan/releases/latest | grep -o -E "https://.+?_Linux_x86_64.tar.gz")" > terrascan.tar.gz
          tar -xf terrascan.tar.gz terrascan && rm terrascan.tar.gz
          sudo install terrascan /usr/local/bin && rm terrascan
          echo "Terrascan version:"
          terrascan version

      - name: Check Terrascan Installation
        run: |
          if [ -f /usr/local/bin/terrascan ]; then
            echo "Terrascan exists:"
            ls -l /usr/local/bin/terrascan
            file /usr/local/bin/terrascan
            echo "Terrascan version:"
            terrascan version
          else
            echo "Terrascan does not exist in /usr/local/bin/"
          fi

      - name: Terrascan - Scan Terraform subdirectories in CPA-Mobile-App
        run: |
          echo "Scanning all Terraform subdirectories under: $GITHUB_WORKSPACE/CPA-Mobile-App"

          SKIP_RULES="AC_AZURE_0391,AC_AZURE_0366"

          find "$GITHUB_WORKSPACE/CPA-Mobile-App" -type f -name "*.tf" -exec dirname {} \; | sort -u | while read dir; do
            echo "Scanning directory: $dir"
          done
